**FREE
ctl-opt dftactgrp(*NO) actgrp(*NEW) option(*SRCSTMT:*NODEBUGIO);

/include INVENTLIB/QRPGLEINC,ITEMSPDS
// ↑ Modern Include (preferred for ILE RPG)

/COPY INVENTLIB/QRPGLEINC,ITEMSPDS
// ↑ Classic Include (still supported in SEU)

/* ITEMS Physical File */
dcl-f ITEMS usage(*input) keyed;

/* Start */
StatusMsg = 'Include / Copy Demo Started';
dsply StatusMsg;

/* Read all items */
setll *start ITEMS;
read ITEMS ItemDS;   // Using fields from copybook

dow not %eof(ITEMS);

   dsply ('Item: ' + %char(ITEMID) + '  ' + %trim(ITEMNAME)
          + ' Stock=' + %editc(QTYONHAND:'J'));

   read ITEMS ItemDS;
enddo;

StatusMsg = 'Include & Copy Demonstration Completed';
dsply StatusMsg;

*inlr = *on;
Return;
